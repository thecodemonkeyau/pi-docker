A bunch of docker configs/scripts for convenience

## Docker

One stop script to install docker on Ubuntu. tested on Resolute

`./install-docker.sh`

## Portainer

Just run the script to deploy portainer. Sets up the swarm, config dir (with permissions) and deploys

`./lock-n-load.sh`

Access the web interface

`https://<hostname>:9443/`
